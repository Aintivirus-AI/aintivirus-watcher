# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (contain secrets)
.env
.env.*
server/.env

# MaxMind GeoLite2 database (large file, download separately)
server/data/*.mmdb

# Runtime visitor history — per-deployment data containing visitor locations,
# written by the server at runtime. Never belongs in the repo.
server/data/visitors-history.json
server/data/visitors-history.json.tmp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
